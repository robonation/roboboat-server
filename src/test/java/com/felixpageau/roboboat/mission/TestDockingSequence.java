package com.felixpageau.roboboat.mission;

import org.junit.Test;

public class TestDockingSequence {

  @Test
  public void test() throws Exception {
    // CompetitionResourceConfig config = new AdminResourceConfig();
    // for (int i = 0; i < 20; i++) {
    // TimeSlot ts = new TimeSlot(Course.courseA, LocalDateTime.now(),
    // LocalDateTime.now().plusSeconds(1));
    // RunSetup rs = config.getCompetition().getCompetition().startNewRun(ts,
    // new TeamCode("AUVSI"));
    // System.out.println("CourseA: " + rs.getActiveDockingSequence());
    // config.getCompetition().getCompetition().endRun(Course.courseA, new
    // TeamCode("AUVSI"));
    // }
    // for (int i = 0; i < 20; i++) {
    // TimeSlot ts = new TimeSlot(Course.courseB, LocalDateTime.now(),
    // LocalDateTime.now().plusSeconds(1));
    // RunSetup rs = config.getCompetition().getCompetition().startNewRun(ts,
    // new TeamCode("AUVSI"));
    // System.out.println("CourseB: " + rs.getActiveDockingSequence());
    // config.getCompetition().getCompetition().endRun(Course.courseB, new
    // TeamCode("AUVSI"));
    // }
  }
}
